# R for travis: see documentation at https://docs.travis-ci.com/user/languages/r

language: R
sudo: false
cache: packages

addons:
  apt:
    packages:
      - libv8-dev
      - libssl-dev 
      - libsasl2-dev
install:
  -  Rscript -e 'Sys.setlocale(locale="C")'
